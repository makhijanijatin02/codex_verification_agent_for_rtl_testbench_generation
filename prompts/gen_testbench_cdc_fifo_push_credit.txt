You are an expert Verilog testbench writer for a dual-clock CDC FIFO with push-side credit return.

PRIMARY GOAL:
- Generate a COMPLETE, self-checking Verilog testbench that is discriminative while remaining faithful to the specification.
- Use only behaviors that are explicitly guaranteed by the spec as strict assertions.
- Do not infer exact cycle-by-cycle cross-domain counter, credit, or synchronizer behavior unless the spec states it explicitly.

REQUIREMENTS:
1. The testbench must be compatible with `iverilog`
2. Include `timescale 1ns/1ps`
3. The module name must be exactly `tb`
4. Generate both clocks and all resets
5. Use self-checking logic with clear pass/fail conditions
6. Print `PASS` only if all checks succeed
7. Always call `$finish`

MANDATORY TESTBENCH SHAPE:
- Generate the testbench as a sequence of named sections in code comments, in this exact order:
  1. `PUSH_RESET_IDLE_CHECKS`
  2. `POP_RESET_IDLE_CHECKS`
  3. `PUSH_SENDER_RESET_HANDSHAKE_CHECKS`
  4. `SINGLE_TRANSFER_CHECKS`
  5. `BACKPRESSURE_HOLD_CHECKS`
  6. `FIFO_ORDERING_CHECKS`
  7. `FILL_AND_DRAIN_CHECKS`
  8. `STALL_BLOCKING_CHECKS`
- Use the section comments literally so an external validator can recognize the structure.
- Do not add extra strict-check sections beyond these eight.

ALLOWED STRICT CHECKS FOR THIS MODULE:
- Idle push-domain reset behavior after qualifying `push_clk` edges:
  - no accepted pushes
  - push-side reset state is active
  - do not require any byte accepted before `push_rst` to remain readable after `push_rst` is asserted; treat push-side reset as a possible queue-invalidation boundary unless retention is explicitly guaranteed
- Idle pop-domain reset behavior after qualifying `pop_clk` edges:
  - no completed pops
  - settled idle reset-safe behavior only after reset has been asserted and enough local `pop_clk` edges have passed
  - do not make strict assertions on `pop_valid` or `pop_data` while pop-side reset behavior is active
  - only check settled post-reset behavior after reset has been asserted, then later released, with enough local `pop_clk` edges elapsed
- `push_sender_in_reset` handshake behavior:
  - when asserted, the push side enters reset behavior
  - when deasserted, the push side can resume normal operation after the reset behavior clears
  - do not require any byte accepted before `push_sender_in_reset` to remain readable after the sender-reset phase unless the specification explicitly guarantees retention
  - do not require an exact one-cycle formula for `push_receiver_in_reset`
  - do not require `push_receiver_in_reset` to equal a sampled delayed reset expression on every push clock
  - do not require `push_receiver_in_reset` itself to assert or deassert as a direct oracle signal
  - only check that reset blocks normal push-side behavior while active and that normal push-side operation can resume after reset behavior clears
- Single transfer:
  - one pushed byte eventually produces exactly one completed pop
  - no duplication
  - ordering preserved
  - do not require the byte to become visibly present on the pop side within a short fixed window before attempting the pop
- Backpressure hold:
  - if `pop_valid=1` and `pop_ready=0`, the same `pop_data` remains visible until the handshake completes
  - if visibility is delayed, wait conservatively for the first valid item before starting the hold check; do not fail simply because it was not visible within a small fixed window
- FIFO ordering for clearly accepted transfers
  - prefer checking ordering on completed pop handshakes with `pop_ready=1`, rather than requiring a separate pre-handshake visibility deadline
- Fill and drain:
  - pushing 17 items without pops eventually causes the FIFO to behave full from the push side
  - draining with `pop_ready=1` eventually produces exactly 17 completed pops in FIFO order
- Stall blocking:
  - while `push_credit_stall=1`, `push_credit` must not assert
- Credit return:
  - after completed pops and with stall deasserted, credit return should be observable eventually
  - do not require `push_credit` to be a one-cycle registered pulse; it is sufficient to check legality and eventual observability
  - do not require a strict one-pulse-per-pop accounting rule on sampled `push_clk` edges
  - do not require an exact pending-credit counter in the testbench
  - do not compare total observed credit pulses against total completed pops

FORBIDDEN STRICT CHECKS FOR THIS MODULE:
- Do not require `push_receiver_in_reset` to equal a one-cycle-delayed or exact sampled formula such as `prev_push_reset_req`
- Do not compare `push_receiver_in_reset` against any per-cycle expected signal like `expected_push_reset`
- Do not assert that `push_receiver_in_reset` must visibly assert after a fixed number of push clocks
- Do not use `push_receiver_in_reset` as a primary pass/fail checker at all
- Do not require exact `push_slots`, `credit_count_push`, `credit_available_push`, or `pop_items` values
- Do not require exact cross-domain visibility latency
- Do not use bounded wait loops like “must happen within N opposite-domain cycles”
- Do not require exact `push_credit` pulse counts derived from reconstructed internal credit arithmetic
- Do not require `push_credit` to have a one-for-one sampled correspondence with completed pops at the testbench level
- Do not assert that total observed `push_credit` pulses must be less than, equal to, or greater than total completed pops
- Do not assert exact `pop_valid` or `pop_data` values during or immediately around `pop_rst`-active behavior; only check settled post-reset behavior
- Do not build a full global counter scoreboard across the two clock domains
- Do not push a byte, assert `push_rst` or `push_sender_in_reset`, and then require that pre-reset byte to pop later as a strict check
- Do not require `pop_valid`/`pop_data` for an expected item to appear within a small fixed number of `pop_clk` edges before the test begins the actual pop attempt
- Do not compare total observed `push_credit` pulses against total completed pop handshakes
- Do not assert that `push_credit` must be high for only a single `push_clk` cycle

ORACLE SHAPE:
- Use a very small reference queue model only for accepted transfer ordering
- Use the queue model only for:
  - bytes successfully pushed under non-reset conditions
  - bytes successfully popped under valid/ready handshakes
- If `push_rst` or `push_sender_in_reset` is asserted, conservatively discard any queued pre-reset expectations unless the specification explicitly guarantees that those bytes survive the reset phase
- Use conservative eventuality checks by waiting a generous fixed simulation window in the scripted scenario, but do not encode “must happen within N CDC latency cycles” as a correctness rule
- For transfer completion, prefer enabling `pop_ready` and waiting for a completed handshake within a generous scenario budget, then checking the dequeued byte on that handshake
- Prefer:
  - reset blocking
  - stall blocking
  - backpressure stability
  - FIFO ordering
  - no duplication / no spontaneous disappearance
  - eventual credit reappearance after pops when stall is not asserted
- For reset-related checks, prefer transaction-level effects:
  - pushes must not be treated as accepted while push-side reset behavior is active
  - pops must not complete while pop-side reset behavior is active
  - after reset release and local clock progress, legal transfers can complete again
  - for `PUSH_RESET_IDLE_CHECKS` and `PUSH_SENDER_RESET_HANDSHAKE_CHECKS`, verify post-reset recovery using fresh post-reset pushes, not by requiring pre-reset queued data to survive the reset phase

SEQUENTIAL SAFETY:
- Keep each helper aligned to exactly one intended cycle
- For synchronous resets, check reset-state outputs only after the active clock edge
- Do not let helper tasks accidentally consume extra clock edges
- For CDC behavior, avoid trying to infer synchronizer stage counts

OUTPUT:
- Return ONLY Verilog code
- Do not include markdown fences
- Do not include explanation
